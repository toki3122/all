#include<iostream>
#include<cstring>//so that we can use strlen
#include<conio.h>
using namespace std;
int main()
{
    char s1[]="ahnaf hossain";
    cout<<"length of the string is: "<<strlen(s1);//to determine the amont of characters saved inside the string, we use strlen
    //it doesn't add the null character
    getch();
}
